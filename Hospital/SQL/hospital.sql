

CREATE TABLE USERINFO (
    ID int NOT NULL AUTO_INCREMENT,
    username varchar(25) NOT NULL,
    firstname varchar(25),
    lastname varchar(25),
    emailid varchar(25),
    phonenumber int(12),
    address varchar(255),
    PRIMARY KEY (ID)
);

